# set true colors
set-option -sa terminal-overrides ",xterm*:Tc"

# set prefix
unbind C-b
set -g prefix C-Space
bind C-Space send-prefix

# binds
bind r command-prompt "rename-window %%"
bind R source-file ~/.config/tmux/tmux.conf
bind h split-window -v -c "#{pane_current_path}"
bind v split-window -h -c "#{pane_current_path}"
bind s choose-session
bind x kill-pane
bind a select-layout main-vertical

bind-key -T copy-mode-vi v send-keys -X begin-selection
bind-key -T copy-mode-vi C-v send-keys -X rectangle-toggle
bind-key -T copy-mode-vi y send-keys -X copy-selection-and-cancel

# general options
set -g main-pane-width 140
set -g base-index 1              # start indexing windows at 1 instead of 0
set -g pane-base-index 1         # start indexing panes at 1 instead of 0
set -g renumber-windows on       # renumber all windows when any window is closed
set -g detach-on-destroy off     # don't exit from tmux when closing a session
set -g escape-time 0             # zero-out escape time delay
set -g history-limit 1000000     # increase history size (from 2,000)
# set -g status-position top       # macOS / darwin style
set -g set-clipboard on          # use system clipboard
set -g pane-active-border-style 'fg=magenta,bg=default'
set -g pane-border-style 'fg=brightblack,bg=default'
setw -g mode-keys vi

# plugins
set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'tmux-plugins/tmux-sensible'
set -g @plugin 'tmux-plugins/tmux-yank'
set -g @plugin 'tmux-plugins/tmux-resurrect'
set -g @plugin 'tmux-plugins/tmux-continuum'
set -g @plugin 'christoomey/vim-tmux-navigator'
# set -g @plugin 'catppuccin/tmux'
# set -g @plugin 'bobcats/everforest-tmux'
# set -g @plugin 'dreamsofcode-io/catppuccin-tmux'

# everforest config
set -g status-justify "left"
set -g status "on"
set -g status-bg default
set -g status-fg colour240
set -g status-left-style "none"
set -g message-command-style "fg=#999f93,bg=#4a555b"
set -g status-right-style "none"
set -g pane-active-border-style "fg=#83c092"
set -g status-style "none,bg=#2b3339"
set -g message-style "fg=#83c092,bg=#4a555b"
set -g pane-border-style "fg=#4a555b"
setw -g window-status-activity-style "none,fg=#83c092,bg=#2b3339"
setw -g window-status-separator ""
setw -g window-status-style "none,fg=#999f93,bg=#2b3339"
set -g status-left ""
# set -g status-right "#[fg=#2b3339,bg=#83c092]   #[fg=#83c092,bg=#374145] #S #[fg=#2b3339,bg=#2b3339,nobold,nounderscore,noitalics] "
set -g status-right "#[fg=#2b3339,bg=#83c092]   #[fg=#83c092,bg=#374145] #S "
setw -g window-status-format "#[fg=#272e33,bg=#7fbbb3] #I #[fg=#d3c6aa,bg=#374145] #W "
setw -g window-status-current-format "#[fg=#272e33,bg=#e69875] #I #[fg=#d3c6aa,bg=#374145] #W "

# catppuccin config
# set -g @catppuccin_window_left_separator ""
# set -g @catppuccin_window_right_separator " "
# set -g @catppuccin_window_middle_separator " █"
# set -g @catppuccin_window_number_position "right"
# set -g @catppuccin_window_default_fill "number"
# set -g @catppuccin_window_default_text "#W"
# set -g @catppuccin_window_current_fill "number"
# set -g @catppuccin_window_current_text "#W#{?window_zoomed_flag,(),}"
# set -g @catppuccin_status_modules_right "directory date_time"
# set -g @catppuccin_status_modules_left "session"
# set -g @catppuccin_status_left_separator  " "
# set -g @catppuccin_status_right_separator " "
# set -g @catppuccin_status_right_separator_inverse "no"
# set -g @catppuccin_status_fill "icon"
# set -g @catppuccin_status_connect_separator "no"
# set -g @catppuccin_directory_text "#{b:pane_current_path}"
# set -g @catppuccin_date_time_text "%H:%M"

# initialize TMUX plugin manager
run '~/.tmux/plugins/tpm/tpm'
